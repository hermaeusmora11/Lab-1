name: C CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install gcc
        run: sudo apt update && sudo apt install -y gcc make

      - name: Build
        run: make all

      - name: Test
        run: make test
